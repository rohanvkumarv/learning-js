// array is obj in js 

const myArr  = [1,3,4,2,true,"raj"]
// heterogenous 
// reziiable
// index starts by zero 
console.log(myArr[0]);
console.log(myArr["one"]); // not allowed 
// and copy like by refrece hota copy nhi bant ahua  

// ways to declare 
const arr = ["iron man  " , "dr.strange"]
const arr1 = new Array("iron man  " , "dr.strange")


// array methods 